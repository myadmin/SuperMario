/**
 * traits/PoleTraveller.ts — ported verbatim from upstream `public/js/traits/PoleTraveller.js`.
 */
import Trait from '../Trait'

export default class PoleTraveller extends Trait {
  distance = 0
}
