{
  "name": "super-mario",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Super Mario Bros. — faithful TypeScript + Phaser 3 port of meth-meth-method/super-mario",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "vite",
    "build": "npm run validate && tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "validate": "node tools/validate-levels.mjs"
  },
  "dependencies": {
    "phaser": "3.90.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  }
}
